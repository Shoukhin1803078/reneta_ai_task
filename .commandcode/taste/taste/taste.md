# Taste
- Prefers concise, single-line commands in scripts over verbose multi-line boilerplate: when the model-pull step in `docker-entrypoint.sh` was a multi-line Python `urllib` block, asked "can i do this only that portion: ollama pull llama3.2:3b" — wanting the same effect expressed as one readable line rather than a long inline script. Confidence: 0.6
- Prefers minimal-diff refactoring: when converting/porting code (e.g., from a Jupyter notebook to .py modules), keep the original code as-is and make only slight adjustments for structure (e.g., wrapping in `main()`, extracting reusable functions), rather than rewriting or modernizing the logic. Explicitly stated: "keep all code as it is ... make slightly changes for adjusting." Reconfirmed when asking for a LangGraph conversion by pasting the exact existing flow code and directing "put these functions like node" — the logic was preserved and restructured around a graph. Confidence: 0.95
- Scopes change requests to exactly what was asked: when updating one part of the UI, wants everything else preserved as-is (explicitly: "please keep citation part as it is") — no unrequested rewrites of nearby markup, styling, or behavior while adopting new ideas from elsewhere. Confidence: 0.6
- Prefers organizing application source code into a dedicated package folder (e.g., `app/`) rather than flat modules at the repo root. Explicitly requested: "create a app folder and move these py files," and expected the moved modules to work as a package (relative imports, `uvicorn app.main:app` / `python -m app.main`). Confidence: 0.7
- Prefers keeping API request/response models (Pydantic schemas) in a dedicated `schemas/` package/folder rather than defined inline in the API module. Explicitly requested: "create a folder called schemas where you put this schema" (AskRequest, Citation, AskResponse), with `main.py` importing from it. Confidence: 0.8
- Prefers keeping actual code in named module files rather than in a package's `__init__.py`; `__init__.py` should only be a thin re-export barrel so existing import paths keep working. Explicitly requested: "create a py file in schemas folder instead of putting schema related code into __init__.py." Confidence: 0.9
- Prefers keeping each module focused on a single responsibility, splitting code into dedicated purpose-named modules instead of letting feature modules accumulate unrelated code: generic helpers → `utils.py` (e.g., `create_context`), rerank-related code → `reranker.py` (e.g., `get_reranker_model`, `rerank_documents`), leaving `retriever.py` with only retrieval functions — with other modules importing from the new modules. Explicitly requested both times: "you can create a utils.py and in this py you can move the create_context function there" and "create another py file called reranker.py where you put rerank related code." Confidence: 0.8
- Prefers keeping project documentation in sync with the actual codebase: after refactors, expects `README.md` and `DESIGN.md` updated to reflect the new layout and responsibilities (including stale details like outdated top-k values in architecture diagrams); also asks for periodic re-reviews of the docs to catch drift against reality — e.g., stale "not committed" notes for files that are committed, structure trees missing real files (verified against actual filenames on disk), and completed work still listed under "what I would improve". Explicitly requested: "update the README.md and DESIGN.md also" and "now again review the README.md and DESIGN.md file if everything is update or not." Continued to review DESIGN.md section-by-section against the code, flagging any section that seems stale: "in Design.md i see the grounding and honesty no 2 point is not updated." Confidence: 0.9
- Wants the README to include a `## Project structure` section with a directory tree of the actual codebase (annotated with each file/folder's role), so the repo layout is visible at a glance. Explicitly requested: "add current codebase structure into README.md also." Confidence: 0.7
- Wants the full project flow captured as visual Mermaid diagrams in a dedicated markdown file (e.g., `FLOW.md`): explicitly requested "give me a flow.md file where you draw like mermaid full flow of this project" — with diagrams covering the high-level overview, ingest pipeline, query/LangGraph flow, API layer (sequence diagram), and Docker deployment, derived from the actual code structure rather than generic boilerplate. Confidence: 0.6
- Prefers design documentation (DESIGN.md) that explains mechanisms clearly and concretely — exact behavior, API contracts on edge cases (e.g., the refusal response with empty `citations`), and trade-offs — rather than vague high-level bullets. Explicitly requested: "update this Grounding and honesty section more clearly in DESIGN.md." Confidence: 0.6
- Prefers documentation written in a natural, first-person, human voice — "The idea is simple…", "I split on those section headings first", "Why I picked it" — as if a person is explaining their own work and reasoning, rather than formal impersonal technical prose. Explicitly requested: "modify the tone of DESIGN.md like i do this like human." Confidence: 0.7
- Values grounded, honest answers in RAG systems: likes that the system answers only from retrieved leaflet chunks and refuses with a fixed phrase ("I don't have that information in the provided documents") plus empty citations when retrieval confidence is too low, rather than guessing or using outside knowledge. Explicitly praised the confidence-based honesty gate: "i like this grounding." Confidence: 0.6
- Wants system quality validated with a concrete, measurable evaluation set (e.g., 5–8 question → expected-answer pairs covering expected source, section, and answer content) run through the pipeline to produce explicit hit-rate metrics, rather than relying on ad-hoc manual checks; also tends to pick items from the docs' "what I would improve" list and ask for them to be implemented. Proposed: "i think we can do this: An evaluation set (5-8 question -> expected-answer pairs) to measure retrieval quality." Confidence: 0.65
- Drives the evaluation set from their own manually-tested queries: records queries they've tried (and the actual answers) in a scratch file (e.g., `sample_evaluation_set.txt`) and asks to merge them into the formal `evaluation_set.json`, with expected answers/sections aligned to what the system actually returned in testing. Expects the set to include out-of-documents/refusal cases too (null expected source/section, expecting the fixed refusal phrase) — not just retrieval hits — and the evaluator to score those refusals. Explicitly: "i already test some query and put it into @sample_evaluation_set.txt please read these and update the evaluation_set.json." Confidence: 0.7
